# Core Flask Framework
flask==3.0.0
flask-cors==6.0.0
gunicorn==22.0.0

# Data Processing
numpy==1.24.3
pillow==10.1.0
matplotlib==3.8.2

# Computer Vision (IMPORTANT: Use headless version for server deployment)
opencv-python-headless==4.8.1.78

# MediaPipe for Movement Analysis
# Version 0.10.8 is stable and compatible
# If this doesn't work, try 0.9.3.0 instead
mediapipe==0.10.8

# AI/ML Libraries
transformers==4.53.0
torch==2.1.1

# Database
supabase==2.0.3

# Utilities
python-dotenv==1.0.0

# Optional: If you need specific versions, uncomment and adjust
# sentencepiece==0.1.99  # Required by some transformers models
# protobuf==3.20.3  # Sometimes needed for mediapipe compatibility
